{
  "name": "publish_skill",
  "display_name": "发布技能",
  "description": "校验并原子发布 Skill 草稿。要求该草稿已有非 block 的 review_skill 审查结论且用户明确确认。创建草稿绝不覆盖；更新草稿执行版本检查；scripts 不会自动注册为工具。",
  "parameters": {
    "type": "object",
    "properties": {
      "draft_id": {
        "type": "string",
        "description": "create_skill 或 update_skill 返回的草稿 ID。"
      },
      "user_confirmed": {
        "type": "boolean",
        "description": "仅当用户看过审查后明确确认发布时才置 true。"
      }
    },
    "required": ["draft_id", "user_confirmed"]
  },
  "permission": "writes",
  "always_loaded": false,
  "load_policy": "group",
  "groups": ["skills"]
}
